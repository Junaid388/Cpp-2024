#include<iostream>

using namespace std;
//class 3
int main(){ // revise the main method
    // cout << "Welcome back to cpp 1" <<endl; //commenting,
    /*
    cout << "Welcome back to cpp 2" <<endl;
    */
    const int uid = 232323; //declared a constant value can't be chaged
    //uid = 33333; //provides error
    int score; //variable declared, not initialised
    score = 110; //variable integer initialised
    int balance = 500;
    cout << "Welcome back to cpp 3" <<endl;
    return 0;
}

// class 4
// Primitive DataType - int, float, double, char, boolean, void,
// Derived - array, function, pointer, reference_wrapper
// user defined -  structure, union, enum, class, typedef